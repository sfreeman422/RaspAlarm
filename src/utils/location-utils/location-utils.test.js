import * as locationUtils from "./location-utils";

it("should return Somerville, NJ when provided with the proper coordinates", () => {
  // Need to build this test based on proper coordinates and calling getUserCity
  expect("Somerville, NJ").toBe("Somerville, NJ");
});
